# Host that serves uploaded store media (product photos, store logos).
# The API returns these as relative paths such as "store-images/<uuid>.jpg".
# Leave unset to fall back to neutral placeholders.
NEXT_PUBLIC_MEDIA_BASE_URL=
